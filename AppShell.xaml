<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiPocketTrainer.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiPocketTrainer"
    xmlns:views="clr-namespace:MauiPocketTrainer.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="MauiPocketTrainer">

    <ShellContent Title="Home"
                 Icon="home.png"
                 ContentTemplate="{DataTemplate views:MainPage}" />
    <ShellContent Title="Weight"
                 Icon="weight.png"
                 ContentTemplate="{DataTemplate views:WeightPage}" />
</Shell>
